function soma(num1, num2) {
    return num1 + num2;
}

console.log(`soma ${soma(15, 16)}`);

const multiplica = (a, b) => a * b;

const falaOla = name => `Olá ${name}`;

console.log(`multiplica ${multiplica(1, 5)}`);
console.log(`Fala olá ${falaOla("Gabriel")}`);